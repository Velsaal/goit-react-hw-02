import React from 'react';
import styles from './Notification.module.css';

const Notification = () => {
  return <p className={styles.notification}>No feedback given yet</p>;
};

export default Notification;
